<ul class="media-list">
  <% newmicroposts.each do |newmicropost| %>
    <% user = newmicropost.user %>
    <li class="media">
      <div class="media-left">
        <img class="media-object img-rounded" src="<%= gravatar_url(user, options = { size: 50 }) %>" alt="">
      </div>
      <div class="media-body">
        <div>
          <%= link_to user.name, user_path(user) %> <span class="text-muted">posted at <%= newmicropost.created_at %></span>
        </div>
        <div>
          <p><%= newmicropost.content %></p>
        </div>
        <div>
          <% if current_user == newmicropost.user %>
            <%= link_to "Delete", newmicropost, method: :delete, data: { confirm: "You sure?" }, class: 'btn btn-danger btn-xs' %>
          <% end %>
        </div>
      </div>
    </li>
  <% end %>
  <%= paginate newmicroposts %>
</ul>